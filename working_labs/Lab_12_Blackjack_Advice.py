print('Lab 12: Blackjack Advice')

import random

'''
cards = {
    'A': 1,
    'J': 10,
    'Q': 10,
    'K': 10,
}

users_card = input("Enter 3 cards separated by commas: ").upper()
users_card = users_card.replace(" ", "")
users_card = users_card.split(",")

total = 0
for card in users_card:
    value = cards.get(card, card)
    value = int(value)
    total += value


if 'A' in users_card:
    if total + 10 <= 21:
        total += 10

    print(value)
''''
card_list = list(card_number)

print(card_list)